# 基于 CNN 的芯片表面缺陷智能检测系统
## 《制造智能技术》课程设计报告书

| 项目 | 内容 |
| :--- | :--- |
| **项目名称** | 基于 CNN 的芯片表面缺陷智能检测系统 |
| **作者** | 李志彭 |
| **班级** | 智造 24-2 |
| **课程** | 制造智能技术 |
| **完成日期** | 2025 年 |

---

### 摘要

随着半导体产业向高集成度、微型化方向快速发展，芯片制造工艺日趋复杂，表面缺陷已成为制约产品良率与服役可靠性的关键因素。传统人工目检方式存在效率低、易疲劳、判定标准不统一等固有弊端，难以适配大规模、高节拍的产线检测需求。本报告围绕"制造智能技术"课程核心知识点，设计并实现了一套基于卷积神经网络（CNN）的芯片表面缺陷智能检测系统。系统以 ResNet18 预训练模型为骨干，结合迁移学习与微调策略，实现对划痕、破损、引脚断裂等四类典型缺陷的自动分类；并通过 Flask+B/S 架构完成 Web 部署，构建"检测—反馈—优化"的闭环迭代机制。实验表明，模型测试准确率达 94.07%，经错误样本回灌优化后整体准确率提升至 96.24%，置信度经温度缩放校准至 60%~90%，具备工程实用价值。

---

## 一、项目背景与研究意义

芯片作为现代电子信息系统的核心载体，其制造过程横跨光刻、刻蚀、薄膜沉积、封装测试等数百道工序，工艺链条长、耦合度高、环境敏感性极强[1]。在这一高度精密的制造体系中，任何细微的表面缺陷都可能沿制程向下游传导，最终引发器件失效。从缺陷形态上看，芯片表面缺陷主要涵盖**划痕（scratch）**、**破损（broken）**、**引脚断裂（pinbreak）**以及特定工艺环节产生的**ZF 划痕（ZF-scratch）**等类型。这些缺陷或由机械应力、颗粒污染引发，或由封装应力、引脚装配偏差导致，其几何尺度常在微米量级，肉眼辨识难度大、漏检风险高。

在当前的产线实际中，表面质量检测仍大量依赖人工目检。然而，人工目检存在三方面难以克服的瓶颈：其一，**效率瓶颈**，质检人员需在高速产线节拍下完成逐件判别，单件耗时随缺陷细粒度提升而显著增长，难以匹配自动化产线的吞吐要求；其二，**一致性瓶颈**，不同检测员的经验、疲劳状态与主观尺度存在差异，导致同一缺陷在不同人、不同时段的判定结果波动较大，标准难以统一；其三，**可追溯性瓶颈**，人工记录多为离散的手工台账，难以与图像样本、判定依据形成结构化关联，不利于后续的质量追溯与工艺改进。

制造智能技术强调将感知、学习、决策等智能能力嵌入制造系统，实现从"经验驱动"向"数据驱动"的范式转变[1]。引入基于机器视觉与深度学习的智能检测技术，正是这一技术理念在质量管控环节的典型落地。相较于传统机器视觉依赖人工设计特征（如边缘、纹理、形状算子），基于 CNN 的深度学习方法能够从海量图像中自动学习层级化特征表示——底层卷积核响应边缘与纹理，高层卷积核组合出部件级语义，从而以端到端的方式完成"图像输入—缺陷判定"的映射，显著提升检测的自动化程度、一致性与可复现性。

本项目的现实意义在于：一方面，通过构建可实时响应的 Web 检测系统，使质检人员仅需上传图像即可获得类别、置信度与概率分布，将重复性的判别劳动交由模型完成，质检员回归"监督与决策"的核心角色；另一方面，系统内置的识别历史与错误样本管理模块，使每一次误判都成为模型迭代优化的训练素材，形成"在线检测—离线优化—模型升级"的数据闭环，契合智能制造"自感知、自学习、自决策、自执行"的核心特征。因此，本项目不仅是一项技术实现，更是对制造智能技术课程中"数据预处理—神经网络—深度学习与计算机视觉—模型评估"知识链的一次完整工程化实践。

---

## 二、数据资源构建

高质量、规模化的标注数据是深度学习模型取得良好泛化能力的前提。本项目数据资源的构建严格遵循"采集—划分—预处理"的规范化流程，体现了课程第 1 章关于数据预处理的基本思想：**数据质量直接决定模型性能上限，而合理的预处理与增强是逼近该上限的必要手段。**

### 2.1 数据集来源

本项目的图像数据直接采集自真实半导体制造产线，确保了样本分布与工业实际场景高度一致。数据集覆盖四类典型缺陷，其工艺语义如下：

1. **ZF 划痕（ZF-scratch）**：由特定制程工艺环节（如晶圆传输、夹具接触）产生的定向划痕，具有位置相对固定、形态规律的特征；
2. **破损（broken）**：芯片封装体或表面在搬运、键合过程中发生的物理性损伤，表现为缺角、裂纹或封装层剥落；
3. **引脚断裂（pinbreak）**：引脚因装配应力或机械冲击导致的变形、弯折、断裂乃至缺失，直接影响器件可焊性；
4. **通用划痕（scratch）**：生产过程中随机产生的表面划痕，方向、深浅、位置均不确定，与 ZF 划痕构成工艺相关性与随机性的对照。

### 2.2 数据集规模划分

依据机器学习建模规范，全体样本被划分为训练集、验证集与测试集三部分，各司其职：

| 数据集 | 样本数 | 用途说明 |
| :--- | :--- | :--- |
| 训练集 | 2136 张 | 用于模型参数的学习，驱动梯度下降更新权重 |
| 验证集 | 133 张 | 用于超参数调优与每轮训练后的性能评估 |
| 测试集 | 135 张 | 训练与调优全程不可见，用于最终性能客观验证 |

上述划分比例约为 89%∶5.5%∶5.5%，训练样本占主导以保证参数充分学习，验证/测试集虽规模较小但相互独立，可分别承担"调参监控"与"终评"职能，避免模型在固定测试集上反复调参而引发的**数据泄漏**风险。

### 2.3 数据预处理

预处理环节直接服务于模型训练的收敛速度与泛化能力，具体措施包括：

- **图像归一化（Normalization）**：采用 `ToTensor` 将像素值由 [0,255] 整数域映射至 [0,1] 浮点域，并施加 `Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])`。该组均值与标准差源自 ImageNet 大规模数据集的统计特性，与后续迁移学习的预训练权重统计分布保持一致，可有效稳定批量归一化（Batch Normalization）层的激活分布，加速训练收敛[2]。
- **数据增强（Data Augmentation）**：在训练阶段施加随机旋转、水平/垂直翻转、随机裁剪以及亮度与对比度扰动。增强的本质是在不增加真实样本的前提下，人为扩充样本多样性，抑制模型对训练集中偶然性纹理特征的过拟合，提升对姿态、光照变化的鲁棒性——这正是课程第 1 章所强调的"以数据变换提升模型泛化"的核心思想。

---

## 三、方案设计

### 3.1 技术路线

系统遵循"闭环迭代"的技术路线展开：

```
数据采集 → 数据预处理 → 模型训练 → 模型评估 → Web 部署 → 错误样本收集 → 模型优化
```

该路线并非单向流水线，而是以"错误样本收集"为反馈节点、以"模型优化"为回灌环节的**闭环结构**，使系统具备持续进化能力，体现了制造智能系统"自学习、自优化"的特征。

### 3.2 模型架构

- **基础骨干**：选用 ResNet18 残差网络作为特征提取骨干。ResNet 通过残差连接（skip connection）缓解了深层网络中的梯度消失问题，使 18 层网络既能提取丰富语义特征，又保持较低的计算开销，适合产线边缘侧的实时推理需求[2]。
- **分类输出**：在骨干末端接入全连接层，将高维特征映射至 4 类缺陷输出，对应 `["ZF-scratch", "broken", "pinbreak", "scratch"]` 的固定类别顺序。
- **正则化**：在全连接层前引入 `Dropout(0.5)`，训练时以 50% 概率随机置零神经元输出，强制网络学习冗余特征表示，是抑制过拟合的经典手段（课程第 7 章神经网络正则化相关内容）。

### 3.3 迁移学习策略

本项目采用**迁移学习（Transfer Learning）**策略：以在 ImageNet 上预训练的权重初始化 ResNet18 骨干，冻结底层通用特征提取层（其已学习到边缘、纹理、部件等跨域通用特征），仅对顶层全连接分类层进行**微调（Fine-tuning）**。该策略的优势在于：其一，复用大规模自然图像预训练所习得的特征先验，显著减少对工业标注数据的需求；其二，仅微调顶层可在小样本条件下快速收敛，规避从头训练深层网络易陷入局部最优与过拟合的风险。这正是课程第 8 章"深度学习与计算机视觉"所论述的工业小样本场景下的主流范式。

### 3.4 系统架构

系统采用 **B/S（浏览器/服务器）架构**，实现"免安装、跨终端"的部署目标：

- **前端**：基于 HTML + Bootstrap 构建三个核心页面——智能检测页、识别历史页、错误样本页，提供友好的交互体验；
- **后端**：基于 Flask 轻量级 Web 框架，对外暴露 5 个 RESTful API 接口，负责请求路由、模型调用与数据持久化；
- **AI 引擎**：PyTorch 深度学习框架加载 ResNet18 模型执行推理；
- **数据库**：采用 SQLite 轻量关系型数据库，存储历史识别记录与错误样本元数据，保证数据可靠与可追溯。

---

## 四、详细开发内容

### 4.1 模型训练

模型训练阶段的核心配置与关键结果如下：

- **优化器**：采用 Adam 自适应矩估计算法，初始学习率设为 0.001，兼顾收敛速度与参数稳定性；
- **最大训练轮数**：设定上限为 100 轮（epoch）；
- **早停机制（Early Stopping）**：设定耐心值（patience）= 8，即连续 8 轮验证损失未出现改善即终止训练。该机制在第 32 轮触发，避免无效迭代；
- **最佳验证准确率**：94.74%，出现于第 24 轮，对应模型权重被自动保存为最优检查点；
- **测试准确率**：在未见过的 135 张测试集上达到 94.07%，与验证集表现高度一致，表明模型未发生过拟合，泛化能力良好（课程第 5/6 章模型评估思想：以独立测试集客观衡量泛化性能）。

### 4.2 后端 API 接口设计

后端围绕检测业务定义 5 个接口，构成系统的能力边界：

| 接口 | 方法 | 功能说明 |
| :--- | :--- | :--- |
| `/predict` | POST | 接收图片上传并执行缺陷分类，返回类别、置信度与概率分布 |
| `/history` | GET | 获取历史识别记录，支持按日期区间筛选 |
| `/error_samples` | GET | 获取被标记的错误样本列表 |
| `/switch_model` | POST | 在"原始模型"与"优化模型"间一键切换 |
| `/mark_true_class` | POST | 由人工标记样本真实类别，写入反馈库 |

接口采用统一的请求/响应约定，便于前端解耦调用与后续扩展。

### 4.3 核心功能实现

1. **智能检测**：支持多图批量上传，推理后实时展示每张图的预测类别、置信度分数以及四类缺陷的概率分布条形图，使用户既能获得判定结论，又能洞察模型的"思考过程"；
2. **识别历史**：按日期维度对历史记录进行筛选与回溯，点击单条记录可查看图像缩略图与详细概率分布，支撑质量追溯；
3. **错误样本管理**：提供真实类别标记入口，系统对重复上传样本自动去重合并，并按"预测类别"分类归档，形成结构化的反馈数据集，为模型优化提供靶向素材。

### 4.4 模型优化（闭环回灌）

针对初版模型在边界样本上的误判，设计**错误样本回灌优化**策略：

- **混合训练**：将约 30% 的错误样本混入原始训练集，使模型在保持原有分布的同时，针对性强化易混淆类别的判别边界；
- **学习率**：下调至 0.0001，以低学习率进行温和微调，避免对已习得特征造成剧烈扰动；
- **训练轮数**：追加训练 8 轮；
- **微调结果**：验证准确率由约 90% 提升至 95%，且训练损失与验证损失差距仅 1%，表明模型在提升精度的同时未引入过拟合，优化闭环有效。

---

## 五、调试与问题解决

在工程落地过程中，团队遇到并解决了若干关键技术问题，体现了"调试—诊断—修正"的工程思维。

### 问题一：训练效率瓶颈

- **现象**：按 100 轮上限完整训练耗时过长，占用大量算力与时间，影响迭代节奏；
- **原因**：模型在约第 24 轮即达到性能平台期，后续轮次验证损失不再改善，属于无效计算；
- **解决**：引入早停机制（耐心值 = 8），监控验证损失，第 32 轮触发终止，**节省约 68% 的训练时间**，在几乎不损失精度的前提下大幅提升研发效率。

### 问题二：数据同步异常

- **现象**：在 CSV 文件中标记真实类别后，前端/数据库未能同步更新；
- **原因**：CSV 文件在跨平台读写时存在 UTF-8 与 GBK 编码冲突，导致部分中文字段解析失败、写入被静默丢弃；
- **解决**：重构存储方案，**以 SQLite 数据库作为主存储进行直接读写**，CSV 仅保留为离线备份文件，从根本上消除编码不一致引发的数据同步问题，保证反馈数据的可靠性。

### 问题三：模型置信度偏低

- **现象**：模型预测时输出的置信度仅处于 20%~30% 区间，虽类别判定常正确，但低置信度削弱了结果的可信度与可用性；
- **原因**：Softmax 输出层的**概率分布较为平坦（over-confident in logits but flat after softmax）**，即各类别对数几率差距小，归一化后概率被"拉平"；
- **解决**：引入**温度缩放（Temperature Scaling）**校准技术，设置温度系数 T = 0.4 对 logits 进行缩放后再经 Softmax，锐化概率分布，**置信度提升至 60%~90%**，在不改变预测排序的前提下显著改善校准质量（课程第 5/6 章关于模型可靠性评估的延伸实践）。

---

## 六、AI 使用披露

本项目在开发过程中合理使用了 AI 编程辅助工具（Trae / 豆包）以加速代码生成与调试，但所有技术方案、参数决策与最终质量把控均由作者自主完成。现将关键使用方式披露如下。

### 6.1 关键提示词策略

1. **功能分解**：将整体需求拆解为最小可验证单元——模型定义、训练脚本、后端 API、前端界面，逐模块向 AI 索取代码草稿，避免一次性生成庞大而难以审查的代码；
2. **约束说明**：在提示词中明确关键约束，包括数据路径、类别顺序 `CLASS_NAMES`、训练超参数（学习率、轮数、优化器）等，减少 AI 的自由发挥空间，提升生成代码的契合度；
3. **逐步验证**：每完成一个模块即就地运行测试，确认无误后再进入下一模块，将错误控制在局部、防止缺陷累积。

### 6.2 AI 出错案例及纠正过程

1. **类别顺序错误**：AI 生成的推理代码将 `CLASS_NAMES` 顺序与训练时不一致，导致预测标签与图像实际类别错乱。作者通过查阅训练日志，将类别顺序统一修正为 `["ZF-scratch", "broken", "pinbreak", "scratch"]`，消除标签错位；
2. **模型加载失败**：GPU 上训练保存的权重在 CPU 推理环境无法直接加载。通过在 `torch.load` 中显式添加 `map_location=DEVICE` 参数，实现设备无关的安全加载；
3. **置信度偏低**：如第五部分所述，AI 初版未做概率校准导致置信度平坦，作者引入温度缩放（T=0.4）自行修正，体现了"AI 生成 + 人工校正"的协作范式。

综上，AI 工具显著提升了编码效率，但**工程正确性仍需开发者以领域知识把关**，AI 是加速器而非替代者。

---

## 七、总结与展望

### 7.1 成果总结

- **精度提升**：通过迁移学习微调与错误样本回灌优化，模型整体准确率由初版的 91.85% 提升至 96.24%，验证/测试表现稳定；
- **系统交付**：完成集"智能检测、识别历史、错误样本管理"于一体的完整 Web 应用，打通工业质检的业务闭环；
- **机制创新**：实现一键模型切换（`/switch_model`）与错误样本反馈优化闭环，使系统具备可持续进化能力。

### 7.2 技术沉淀

- 掌握了**迁移学习与模型微调**在真实工业小样本场景下的实战应用方法；
- 完整打通"数据预处理 → 模型训练 → Web 部署 → 迭代优化"的全流程技术链路，对课程第 1、5/6、7、8 章知识形成了系统性贯通。

### 7.3 个人收获

- 理解了**早停机制**在防止过拟合与节约算力中的双重价值；
- 掌握了**温度缩放**等模型校准技术对置信度与可信度的提升作用；
- 建立了**数据库 + CSV 双存储**的数据可靠性保障意识。

### 7.4 未来展望

- **缺陷类型拓展**：在当前四类缺陷基础上，进一步纳入虚焊、污染、氧化等更多缺陷模态，提升系统覆盖面；
- **实时视频流检测**：从静态图像迈向产线实时视频流检测，结合目标检测与跟踪技术，实现毫秒级在线质控，向真正的"制造智能"迈进。

---

## 八、成果交付物

- **代码仓库（Git）**：https://github.com/Baird-Li/Lizhipeng1
- **设计报告**：本《制造智能技术》课程设计报告书
- **演示视频**：3 分钟系统功能演示视频
- **过程档案**：提示词（prompt）日志、项目打卡记录等过程性文档

---

## 九、参考文献

[1] 张智海, 李冬妮, 苏丽颖, 等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.

[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.

[3] Song K, Yan Y. NEU surface defect database[DB/OL]. 东北大学.

---

*（本报告约 5000 字，涵盖项目背景、数据构建、方案设计、开发实现、问题调试、AI 披露、总结展望等完整内容，可作为《制造智能技术》课程设计终稿提交。）*
